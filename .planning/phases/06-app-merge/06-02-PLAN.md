---
phase: 06
plan: 02
title: "AndroidManifest Merge"
wave: 1
depends_on: []
requirements_addressed: [MERG-03]
files_modified:
  - app/src/main/AndroidManifest.xml
autonomous: true
estimated_tasks: 2
---

# Plan 02: AndroidManifest Merge

<objective>
Add SOUL app's required permissions (READ_CONTACTS, READ_CALENDAR) and service declarations (ForegroundService for flutter_foreground_task, NotificationListenerService) to the soul-terminal AndroidManifest.xml. After this plan, the merged manifest supports all SOUL features alongside existing Termux services.
</objective>

<must_haves>
- `READ_CONTACTS` permission present in soul-terminal AndroidManifest.xml
- `READ_CALENDAR` permission present in soul-terminal AndroidManifest.xml
- `flutter_foreground_task.service.ForegroundService` service declared with `specialUse` type and AI assistant subtype
- `notification.listener.service.NotificationListener` service declared with `BIND_NOTIFICATION_LISTENER_SERVICE` permission
- Existing TermuxService, RunCommandService, SystemEventReceiver declarations unchanged
- No duplicate permission entries
</must_haves>

## Tasks

<task id="06-02-01">
<title>Add SOUL permissions to soul-terminal AndroidManifest.xml</title>

<read_first>
- /data/data/com.termux/files/home/soul-terminal/app/src/main/AndroidManifest.xml
- ~/soul-app/apps/soul/android/app/src/main/AndroidManifest.xml
</read_first>

<action>
Add two missing permissions to `app/src/main/AndroidManifest.xml`. Insert after the existing `<uses-permission android:name="com.android.alarm.permission.SET_ALARM" />` line (line 39), before the `<application` tag:

```xml
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
```

Do NOT add permissions that already exist in the manifest:
- FOREGROUND_SERVICE — already present (line 30)
- FOREGROUND_SERVICE_SPECIAL_USE — already present (line 13)
- RECEIVE_BOOT_COMPLETED — already present (line 37)
- POST_NOTIFICATIONS — already present (line 14)
- WAKE_LOCK — already present (line 29)
- MANAGE_EXTERNAL_STORAGE — already present (line 27)
- INTERNET — already present (line 24)
- ACCESS_NETWORK_STATE — already present (line 23)
</action>

<acceptance_criteria>
- `grep "READ_CONTACTS" app/src/main/AndroidManifest.xml` returns a match
- `grep "READ_CALENDAR" app/src/main/AndroidManifest.xml` returns a match
- `grep -c "FOREGROUND_SERVICE\"" app/src/main/AndroidManifest.xml` returns exactly 1 (no duplicate)
- `grep -c "WAKE_LOCK" app/src/main/AndroidManifest.xml` returns exactly 1 (no duplicate)
</acceptance_criteria>

<automated>
grep -q "READ_CONTACTS" app/src/main/AndroidManifest.xml && grep -q "READ_CALENDAR" app/src/main/AndroidManifest.xml && test "$(grep -c 'android.permission.FOREGROUND_SERVICE"' app/src/main/AndroidManifest.xml)" -eq 1 && echo "PASS" || echo "FAIL"
</automated>
</task>

<task id="06-02-02">
<title>Add SOUL service declarations to soul-terminal AndroidManifest.xml</title>

<read_first>
- /data/data/com.termux/files/home/soul-terminal/app/src/main/AndroidManifest.xml
- ~/soul-app/apps/soul/android/app/src/main/AndroidManifest.xml
</read_first>

<action>
Add two service declarations inside the `<application>` tag of `app/src/main/AndroidManifest.xml`. Insert after the existing `RunCommandService` closing `</service>` tag (around line 217), before the Samsung DeX meta-data section:

```xml
        <!-- SOUL AI foreground service (flutter_foreground_task plugin) -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="AI assistant providing proactive briefings and contextual monitoring" />
        </service>

        <!-- SOUL notification listener (notification_listener_service plugin) -->
        <service
            android:label="SOUL Notifications"
            android:name="notification.listener.service.NotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
```

These services coexist safely with existing TermuxService and RunCommandService because:
- Different Java class names
- Different foregroundServiceType subtypes (TermuxService = "terminal_emulator", SOUL = "AI assistant...")
- Different notification channel IDs (SOUL uses "soul_foreground" configured in Dart, Termux uses native Java channel)
</action>

<acceptance_criteria>
- `grep "flutter_foreground_task.service.ForegroundService" app/src/main/AndroidManifest.xml` returns a match
- `grep "notification.listener.service.NotificationListener" app/src/main/AndroidManifest.xml` returns a match
- `grep "BIND_NOTIFICATION_LISTENER_SERVICE" app/src/main/AndroidManifest.xml` returns a match
- `grep "AI assistant" app/src/main/AndroidManifest.xml` returns a match
- `grep "TermuxService" app/src/main/AndroidManifest.xml` still returns a match (not removed)
- `grep "RunCommandService" app/src/main/AndroidManifest.xml` still returns a match (not removed)
- XML is well-formed (no unclosed tags)
</acceptance_criteria>

<automated>
grep -q "flutter_foreground_task.service.ForegroundService" app/src/main/AndroidManifest.xml && grep -q "notification.listener.service.NotificationListener" app/src/main/AndroidManifest.xml && grep -q "TermuxService" app/src/main/AndroidManifest.xml && grep -q "RunCommandService" app/src/main/AndroidManifest.xml && echo "PASS" || echo "FAIL"
</automated>
</task>

## Verification

<verify>
1. `grep -c "READ_CONTACTS\|READ_CALENDAR\|ForegroundService\|NotificationListener" app/src/main/AndroidManifest.xml` yields >= 4
2. Existing Termux services (TermuxService, RunCommandService) and receivers (SystemEventReceiver) are unchanged
3. No duplicate permission declarations
4. XML is well-formed (parseable)
</verify>
