---
phase: 06
plan: 01
title: "Code Copy & Dependency Merge"
wave: 1
depends_on: []
requirements_addressed: [MERG-01, MERG-02]
files_modified:
  - flutter_module/lib/ (237 Dart files copied)
  - flutter_module/lib/objectbox-model.json
  - flutter_module/lib/objectbox.g.dart
  - flutter_module/pubspec.yaml
  - flutter_module/assets/config/default_intervention_config.yaml
autonomous: true
estimated_tasks: 5
---

# Plan 01: Code Copy & Dependency Merge

<objective>
Copy all 237 SOUL app Dart files from ~/soul-app/apps/soul/lib/ into flutter_module/lib/, merge the soul_core stub inline, merge all 52 dependencies into flutter_module/pubspec.yaml, copy assets, and verify `flutter pub get` resolves without errors.
</objective>

<must_haves>
- All 237 Dart files from SOUL app are present under flutter_module/lib/
- soul_core stub merged inline in flutter_module/lib/core/
- objectbox-model.json and objectbox.g.dart present at flutter_module/lib/
- flutter_module/pubspec.yaml contains all 52 production deps + 8 dev deps
- Assets directory flutter_module/assets/config/ exists with default_intervention_config.yaml
- `pigeon: ^22.7.0` retained as runtime dependency
- `module:` section in pubspec.yaml preserved unchanged
- Pigeon generated files in flutter_module/lib/generated/ untouched
- SDK constraint updated to `^3.11.1`
</must_haves>

## Tasks

<task id="06-01-01">
<title>Copy SOUL app Dart source tree to flutter_module/lib/</title>

<read_first>
- ~/soul-app/apps/soul/lib/ (directory listing)
- /data/data/com.termux/files/home/soul-terminal/flutter_module/lib/ (current contents)
</read_first>

<action>
Copy the following directories from `~/soul-app/apps/soul/lib/` to `flutter_module/lib/`:
- `core/` (di/, router/, sentry_config.dart, theme/, utils/)
- `models/`
- `services/`
- `ui/`

Do NOT copy `main.dart` yet (handled in Plan 03).
Do NOT overwrite `flutter_module/lib/generated/` — this directory contains Pigeon generated code that must be preserved.

Commands:
```bash
cp -r ~/soul-app/apps/soul/lib/core flutter_module/lib/
cp -r ~/soul-app/apps/soul/lib/models flutter_module/lib/
cp -r ~/soul-app/apps/soul/lib/services flutter_module/lib/
cp -r ~/soul-app/apps/soul/lib/ui flutter_module/lib/
```
</action>

<acceptance_criteria>
- Directory `flutter_module/lib/core/` exists with `di/`, `router/`, `theme/` subdirectories
- Directory `flutter_module/lib/services/ai/` exists
- Directory `flutter_module/lib/ui/chat/` exists
- `flutter_module/lib/generated/terminal_bridge.g.dart` still exists (not overwritten)
- `flutter_module/lib/generated/system_bridge.g.dart` still exists (not overwritten)
- `find flutter_module/lib -name "*.dart" | wc -l` yields >= 238 (236 copied + 2 Pigeon generated)
</acceptance_criteria>

<automated>
test -d flutter_module/lib/core/di && test -d flutter_module/lib/services/ai && test -d flutter_module/lib/ui/chat && test -f flutter_module/lib/generated/terminal_bridge.g.dart && echo "PASS" || echo "FAIL"
</automated>
</task>

<task id="06-01-02">
<title>Copy ObjectBox generated files to flutter_module/lib/</title>

<read_first>
- ~/soul-app/apps/soul/lib/objectbox-model.json
- ~/soul-app/apps/soul/lib/objectbox.g.dart
- /data/data/com.termux/files/home/soul-terminal/flutter_module/lib/ (current contents)
</read_first>

<action>
Copy the ObjectBox generated/schema files:
```bash
cp ~/soul-app/apps/soul/lib/objectbox-model.json flutter_module/lib/
cp ~/soul-app/apps/soul/lib/objectbox.g.dart flutter_module/lib/
```

These files must be at `flutter_module/lib/` root — same level as `main.dart` — because `objectbox.g.dart` is imported as `'objectbox.g.dart'` (relative) from `main.dart`.
</action>

<acceptance_criteria>
- `flutter_module/lib/objectbox-model.json` exists
- `flutter_module/lib/objectbox.g.dart` exists
- `grep "openStore" flutter_module/lib/objectbox.g.dart` returns matches
</acceptance_criteria>

<automated>
test -f flutter_module/lib/objectbox-model.json && test -f flutter_module/lib/objectbox.g.dart && grep -q "openStore" flutter_module/lib/objectbox.g.dart && echo "PASS" || echo "FAIL"
</automated>
</task>

<task id="06-01-03">
<title>Merge soul_core stub into flutter_module/lib/core/</title>

<read_first>
- ~/soul-app/packages/soul_core/lib/soul_core.dart
- /data/data/com.termux/files/home/soul-terminal/flutter_module/lib/core/ (directory listing)
</read_first>

<action>
The soul_core package contains only one file: `soul_core.dart` with an empty library declaration. It's referenced nowhere in the SOUL app lib/ code (all real code is in `apps/soul/lib/`).

Verify no files in `flutter_module/lib/` import `package:soul_core/`:
```bash
grep -r "package:soul_core/" flutter_module/lib/
```

If no imports found (expected), skip copying — soul_core is dead code. If any imports are found, create `flutter_module/lib/core/soul_core.dart` with the same content and update imports.
</action>

<acceptance_criteria>
- No file in `flutter_module/lib/` imports `package:soul_core/`
- If soul_core.dart was copied: it exists at `flutter_module/lib/core/soul_core.dart`
</acceptance_criteria>

<automated>
grep -r "package:soul_core/" flutter_module/lib/ 2>/dev/null; test $? -ne 0 && echo "PASS: no soul_core imports" || echo "FAIL: soul_core imports found, need migration"
</automated>
</task>

<task id="06-01-04">
<title>Copy SOUL app assets to flutter_module/</title>

<read_first>
- ~/soul-app/apps/soul/assets/config/default_intervention_config.yaml
- /data/data/com.termux/files/home/soul-terminal/flutter_module/pubspec.yaml
</read_first>

<action>
Create the assets directory and copy the intervention config:
```bash
mkdir -p flutter_module/assets/config/
cp ~/soul-app/apps/soul/assets/config/default_intervention_config.yaml flutter_module/assets/config/
```

The `flutter:` assets section will be added in the next task (pubspec merge).
</action>

<acceptance_criteria>
- `flutter_module/assets/config/default_intervention_config.yaml` exists
</acceptance_criteria>

<automated>
test -f flutter_module/assets/config/default_intervention_config.yaml && echo "PASS" || echo "FAIL"
</automated>
</task>

<task id="06-01-05">
<title>Merge pubspec.yaml with all 52 SOUL dependencies</title>

<read_first>
- /data/data/com.termux/files/home/soul-terminal/flutter_module/pubspec.yaml
- ~/soul-app/apps/soul/pubspec.yaml
</read_first>

<action>
Replace `flutter_module/pubspec.yaml` with the merged version. Critical rules:
1. Keep `name: flutter_module` (NOT soul_app)
2. Keep the `module:` section at the bottom (androidPackage, iosBundleIdentifier)
3. Update SDK constraint from `>=3.4.0 <4.0.0` to `^3.11.1`
4. Keep `pigeon: ^22.7.0` as a runtime dependency
5. Add all 41 SOUL production dependencies (excluding `flutter:` which exists)
6. Add all 8 SOUL dev dependencies
7. Add `assets:` section under `flutter:`

The merged pubspec.yaml must contain these production dependencies (in addition to `flutter` and `pigeon`):
- flutter_riverpod: ^3.3.1
- riverpod_annotation: ^4.0.2
- anthropic_sdk_dart: ^1.3.2
- drift: ^2.31.0
- sqlite3_flutter_libs: ^0.5.0
- flutter_markdown_plus: ^1.0.7
- markdown: ^7.3.0
- flutter_highlight: ^0.7.0
- highlight: ^0.7.0
- dynamic_color: ^1.8.1
- share_plus: ^10.0.0
- flutter_secure_storage: ^10.0.0
- envied: ^1.3.3
- go_router: ^17.1.0
- freezed_annotation: ^3.0.0
- json_annotation: ^4.9.0
- logger: ^2.5.0
- mcp_dart: ^2.1.0
- path_provider: ^2.1.0
- uuid: ^4.5.0
- connectivity_plus: ^6.0.0
- glob: ^2.1.3
- google_fonts: ^6.2.1
- intl: ^0.20.2
- objectbox: ^5.2.0
- objectbox_flutter_libs: any
- http: ^1.4.0
- url_launcher: ^6.3.0
- flutter_foreground_task: ^9.2.1
- flutter_local_notifications: ^19.0.0
- flutter_contacts: ^1.1.9+2
- device_calendar_plus: ^0.3.4
- notification_listener_service: ^0.3.5
- permission_handler: ^11.3.0
- device_info_plus: ^11.3.0
- sentry_flutter: ^9.0.0
- diff_match_patch: ^0.4.1
- yaml: ^3.1.0
- xterm: ^4.0.0
- web_socket_channel: ^3.0.3

Dev dependencies to add:
- build_runner: ^2.4.0
- drift_dev: ^2.31.0
- freezed: ^3.2.5
- json_serializable: ^6.8.0
- riverpod_generator: ^4.0.3
- envied_generator: ^1.3.3
- flutter_lints: ^6.0.0
- objectbox_generator: any
</action>

<acceptance_criteria>
- `grep "name: flutter_module" flutter_module/pubspec.yaml` succeeds
- `grep "pigeon:" flutter_module/pubspec.yaml` succeeds
- `grep "flutter_riverpod:" flutter_module/pubspec.yaml` succeeds
- `grep "objectbox:" flutter_module/pubspec.yaml` succeeds
- `grep "drift:" flutter_module/pubspec.yaml` succeeds
- `grep "flutter_secure_storage:" flutter_module/pubspec.yaml` succeeds
- `grep "sentry_flutter:" flutter_module/pubspec.yaml` succeeds
- `grep "build_runner:" flutter_module/pubspec.yaml` succeeds
- `grep "objectbox_generator:" flutter_module/pubspec.yaml` succeeds
- `grep "androidPackage: com.soul.terminal.flutter_module" flutter_module/pubspec.yaml` succeeds
- `grep "default_intervention_config.yaml" flutter_module/pubspec.yaml` succeeds
- `grep "sdk: \^3.11.1" flutter_module/pubspec.yaml` succeeds (or equivalent >=3.11.1)
</acceptance_criteria>

<automated>
cd flutter_module && grep -q "name: flutter_module" pubspec.yaml && grep -q "pigeon:" pubspec.yaml && grep -q "flutter_riverpod:" pubspec.yaml && grep -q "objectbox:" pubspec.yaml && grep -q "drift:" pubspec.yaml && grep -q "build_runner:" pubspec.yaml && grep -q "androidPackage:" pubspec.yaml && grep -q "default_intervention_config" pubspec.yaml && echo "PASS" || echo "FAIL"
</automated>
</task>

## Verification

<verify>
1. `find flutter_module/lib -name "*.dart" | wc -l` yields >= 238 files
2. `flutter_module/lib/generated/terminal_bridge.g.dart` and `system_bridge.g.dart` unchanged
3. `flutter_module/pubspec.yaml` contains `name: flutter_module`, `module:` section, all 52+ dependencies
4. `flutter_module/assets/config/default_intervention_config.yaml` exists
5. No `package:soul_app/` or `package:soul_core/` imports remain in `flutter_module/lib/`
</verify>
